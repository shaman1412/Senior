<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.Senior.Faff">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="Faff">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Promotion.PromotionActivity" />
        <activity android:name=".Promotion.PromotionView" />
        <receiver android:name=".broadcast_to_start">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <!--
                 <intent-filter>
             <action android:name="android.intent.action.MAIN" />


             <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>
        -->
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".LocationProvider" />
        <activity android:name=".TestActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".PartyActivity" />
        <activity android:name=".UserProfile.InsertUserProfile" />

        <!-- <activity -->
        <!-- android:name=".ServerSocketActivity" -->
        <!-- android:label="@string/title_activity_server_socket" -->
        <!-- android:theme="@style/AppTheme"></activity> -->
        <activity
            android:name=".UserProfile.ShowUserprofile"
            android:parentActivityName=".Main2Activity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Main2Activity" />
        </activity>
        <activity android:name=".ServerSocketActivity" />
        <activity android:name=".ClientSocketActivity" />
        <activity android:name=".FilterActivity" />
        <activity android:name=".ImagerActivity" />
        <activity android:name=".utils.PlacePicker" />
        <activity android:name=".chat.ChatMainActivity" />
        <activity android:name=".chat.Chat_Room" />
        <activity
            android:name=".RestaurantProfile.RestaurantMapsActivity"
            android:label="@string/title_activity_restaurant_maps" />
        <activity android:name=".RestaurantProfile.Add_RestaurantProfile" />
        <activity
            android:name=".TestMapsActivity"
            android:label="@string/title_activity_test_maps" />
        <activity android:name=".firebasetestActivity" />
        <activity android:name=".Fragment.Party.Party_CreateNewParty" />
        <activity
            android:name=".Fragment.Party.Party_CreateNewParty_map"
            android:label="@string/title_activity_party__create_new_party_map" />
        <activity android:name=".Promotion.PromotionShow" />
        <activity android:name=".chat.TestFirebaseStorageActivity" />
        <activity android:name=".chat.CreateParty" />
        <activity android:name=".chat.ParttyDetail" />
        <activity android:name=".RestaurantProfile.Show_RestaurantProfile" />
        <activity
            android:name=".testlife"
            android:configChanges="orientation|screenSize|keyboardHidden" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".TestAPI" />

        <activity android:name=".UserProfile.UpdateUserProfile" />
        <activity android:name=".RestaurantProfile.Edit_RestaurantProfile" />
        <activity android:name=".Fragment.Party.Show_party_profile" />
        <activity android:name=".UserProfile.favorite_restaurant" />
        <activity
            android:name=".Add_map"
            android:label="@string/title_activity_add_map" />
        <activity android:name=".UserProfile.Change_password" />
        <activity android:name=".testnoti" />
        <activity android:name=".RestaurantProfile.Add_Comment" />
        <activity android:name=".chat.Chat_Party" />
        <activity android:name=".test_callservice" />

        <service android:name=".server_service" />

    </application>

</manifest>